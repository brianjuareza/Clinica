LO QUE DEBEMOS TENER INSTALADO PARA EJECUTAR TODO EL APP.PY(Todo esto tambien lo podemos instalar en un ENV siempre y cuando este activado)

Principal:
	Python3
	Flask
	Mysql-Server

Librerias:
	pip install Flask-login(autentificacion)
	pip install Flask-Mail(envio de correos) , 'OJO: Configurar en nuestra cuenta el generar un token para apps de terceros y tener la verificacion en 2 pasos activa'
	
	pip install mysql-connector-python
	pip install pdfkit
	pip install PyMySQL
	sudo apt-get install wkhtmltopdf
	
